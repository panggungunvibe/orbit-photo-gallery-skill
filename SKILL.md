---
name: orbit-photo-gallery
description: 配合已安装并授权的夸克网盘 Skill，把指定相册或照片文件夹整理成旅行记忆网站。当用户要求“把相册整理成旅行记忆”、用网盘照片做旅行网站或更新相册网站时使用。先看图去重、筛选并确认能自然形成5–10类，再编写和运行带分类圆环、照片长卷、日记、收藏、分类下载和手势控制的网站；素材不足则请用户补充。
---

# Orbit Photo Gallery

用户安装并授权 **夸克网盘 Skill（quarkclouddrive）**，再安装本 Skill 后，只需说：

> 帮我把夸克网盘里的「云南旅行」相册整理成旅行记忆。

这表示读取指定相册，先完成真实照片的视觉筛选与分类，再制作并运行网站。用户无需下载照片、写 JSON 或执行工程命令。默认交付本地预览，不自动发布个人照片。明确提供本地素材时跳过夸克连接，其他规则相同。

## 工作流程：先验素材，再生成网站

1. **连接并获取指定相册。** 读取当前环境安装的夸克 Skill，沿用已明确的账号与相册。获取完整结果列表并下载到独立的私人素材目录，不能把工具预览的前几项当作全部。详细规则见 [夸克流程](references/quark-workflow.md)。不移动、重命名或删除网盘原片。
2. **先看图、去重、分类。** 执行下文选片规则，查看全部联系表并放大候选原片。记录入选和排除理由，按实际画面生成清单；不要先建站再拿演示图或重复照片填空。
3. **检查 5–10 类。** 经过筛选后，必须自然形成 **5–10 个非空、含义不同的分类，优先 6–8 类**。超过 10 类时按语义合并相近主题至范围内，不直接丢弃多出的类目或照片；照片增加时优先丰富各类长卷，不持续增加圆环类别。每张照片只能属于一类。不能硬拆同类、重复用图、给同一类换名或保留劣质照片凑数。少于 5 类时停止生成，说明当前入选数量与实际类目，并告诉用户：“网盘素材不足以组成至少 5 类的旅行圆环，请补充不同场景或类型的照片后再整理。”可根据已见素材说明缺少哪些类型；不要保证增加几张就一定足够。更新已有网站也要先检查；不足时保留原网站，不覆盖它。
4. **通过预检后才创建或更新工程。** 按 [清单格式](references/manifest.md) 由智能体编写私人 `selection.json`，执行 `python scripts/import_photos.py selection.json --check`。它验证 5–10 个不同名称的非空分类、精确重复、方向与清单内容；语义是否自然、近似照片和最佳表情仍由视觉判断。预检失败先修复，素材不足则返回上一步提示。
5. **生成完整网站。** 新项目执行 `python scripts/scaffold.py <新项目目录>`，复用 `assets/template/` 的完整效果和功能。智能体安装依赖，执行 `python scripts/import_photos.py selection.json --project <项目目录>`，修改网站标题、分类简介和文案。更新时沿用现有项目与已确认内容。不要将私人素材或清单放进 Skill 安装目录。
6. **运行、验证与交付。** 完成下文验收，给用户可打开的预览、入选数量和分类概览。完成标准是真实照片构成的可运行网站，不能以搜索结果、建议、代码片段、空模板或手工教程代替。

## 选片与分类规则

运行 `python scripts/review_photos.py <照片目录> <独立审阅目录>` 生成索引与联系表。依赖在 `requirements.txt`；macOS 可用系统转换 HEIC，其他平台需 `pillow-heif`。

- 查看全部联系表，再放大人物面部、连拍候选和可疑模糊区域；仅靠缩略图不能判断清晰度。
- 先应用 EXIF 方向，默认只选竖图，排除横图和方图。只有用户明确接受竖幅裁切时才使用经视觉检查的裁切，不能为了达标自动裁横图，不能切掉人物头部或合照成员。
- 同场景近似连拍综合清晰度、表情、姿态、遮挡和构图，通常只留最好的一张。排除明显失焦、抖动、非刻意闭眼或不自然表情的失败片；不要把背景虚化、合理剪影或有意的表情当成缺陷。
- 跨批次比较原图、压缩副本、带字版本和拼图中的重复画面。背景相同但主体、事件或视角明显不同的照片可保留。脚本只检查精确解码像素重复，不能替代近似去重与视觉选择。
- 按素材使用直观类名，例如海景、建筑、街景、夜景、花草、单人照、合照。例子不是必凑清单。类别总数限定 5–10，优先 6–8；不要使用“窗前与镜中”等刻意细分来凑数。
- 一张照片只在一个类目的长卷出现；每类选一张代表图做封面，封面可以复用该类长卷内的一张照片，不算跨类重复。

保留本地入选／排除理由，不改原片。新批次与旧批次一起去重；复用可确认未变的已下载素材、文案和照片身份。

## 文案与照片处理

标题简短文艺，分类名直观。描述为日记风：风景侧重真实意象；人物描述可见状态、神情，心情以“像是”“也许”表示推测，不编造身份、地点或经历。默认原创；不得把原创句子冒充名家摘录。已有文案除非换图或用户要求，否则保留。

导入脚本校正方向、验证竖幅和精确去重，输出最长边 1800px、去 EXIF 的 WebP。下载功能导出这些精选网站图片的 JPG，不能称为网盘原图。

## 网站必须保留的效果与功能

从模板生成 **分类圆环 → 横向照片长卷 → 居中大图与日记**。暖米白、海蓝与夕阳色光晕、轨道线、细字分类和轻阴影保持一致。详细维护规则见 [网站功能与验收](references/template.md)。

- 圆环下方用细字显示分类；鼠标拖动、左右键旋转，滚轮和触屏双指缩放。点击分类进入对应长卷。
- 长卷按竖图真实比例等高排布，可拖动、滚轮和左右键浏览；返回保留圆环状态，再进同类保留滚动位置。
- 点击照片在页面中间放大，展示完整照片、名字、日记、收藏和单张保存；不要恢复成右侧小抽屉。详情阻止底层继续滚动。
- 收藏夹独立入口、数量、取消收藏、空状态和本浏览器持久保存；收藏列表可批量下载。
- 桌面一键按分类文件夹下载全部精选照片 ZIP，也可按分类打包收藏照片。显示准备进度与失败提示。
- 手机保留完整的立体圆环、长卷、详情和收藏，隐藏开启手势控制入口。圆环需按完整占幅缩放卡片，并保留后半圈可见度与俯视角，不能看起来只是横排卡片。长卷使用浏览器原生横向触控（`touch-action: pan-x`），触屏不能进入鼠标拖拽逻辑或捕获触点；触摸开始先取消旧缓动，避免左右滑不动。保存入口准备照片后，在支持文件分享的浏览器调用系统菜单，由用户选择保存图像；不支持或批量失败时提供可长按保存的图片和单张保存。不能承诺网页静默写入系统相册，也不能把 ZIP 下载称为相册保存。

## 最新手势约定

复用模板实现，不恢复旧版捏合点击、V 手势、点赞点击或食指静止停留打开。

- **张掌左右移动**：连续控制圆环或长卷位置；停手、收手或丢失追踪立即停止，不积累惯性或缓动。
- **单食指伸出再弯回**：单独伸出食指稳定约 160ms，界面提示“可以点按”并标出当前目标，再弯回食指完成点击。圆环打开正前方分类，长卷放大距屏幕中心最近的照片。直接收起手掌、捏合、静止指向和丢手不点击；不要求朝镜头前推。
- **双手交叉成 X**：返回一层，保持交叉不连续返回；识别包含手腕下方的交叉区域，并容忍短暂追踪丢失。
- **五指张开、掌心朝镜头，前后移动**：靠近放大、远离缩小；圆环、长卷和详情都支持。用掌心表观大小作为相对距离依据，不声称厘米测距。左右位移优先滚动，小幅尺寸抖动不缩放，丢手后重建基准。

在项目执行 `npm run setup:hands` 下载固定版本模型并校验哈希。用户明确不要手势时可跳过；鼠标和触屏不依赖模型。点击开关后才申请摄像头，关闭后释放轨道；手势需要 HTTPS 或 localhost，图像在浏览器本地处理。页面显示实时识别反馈。

## 验收与交付

执行 `npm test`、`npm run build`（构建也检查 5–10 类范围）。在浏览器检查桌面与手机：圆环、长卷、居中详情、返回、收藏刷新保留、取消收藏、分类 ZIP 和手机保存入口。实际下载 ZIP 并核对分类目录与照片数量。不得用测试数据代替用户图片完成交付。

开发服务下的 `evidence/interaction-check.html` 验证合成关键点驱动真实控制器；它不能证明真人摄像头识别可靠。分别说明已测试的浏览器、模拟交互和未实测的真人手势／手机系统相册保存。

只在用户明确要求公开时才部署或上传个人网站。发布 Skill 模板时仅包含原创演示素材、通用代码和说明；照片、选片清单、授权信息、审阅截图均不随模板上传。
