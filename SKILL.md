---
name: orbit-photo-gallery
description: 配合已安装并授权的夸克网盘 Skill，将用户指定的网盘相册或照片文件夹直接制作成旅行记忆网站。当用户说“把某个相册整理成旅行记忆”“用网盘照片做旅行网站”或要求更新旅行记忆时使用；自动读取照片、选片去重、分类写日记、编写并运行带 3D 圆环、照片长卷和手势控制的网站。
---

# Orbit Photo Gallery

默认与 **夸克网盘 Skill（quarkclouddrive）** 配合。用户装好并授权夸克网盘 Skill，再安装本 Skill 后，只需说：

> 帮我把夸克网盘里的「云南旅行」相册整理成旅行记忆。

这句话表示：读取指定相册，直接制作并运行一个可以浏览的旅行记忆网站。完成标准是用户可以打开真实照片构成的网站；不能只返回网盘搜索结果、整理建议、代码片段、空模板或让用户自己执行的教程。

网站采用「分类圆环 → 分类照片长卷 → 照片与日记」结构。模板在 `assets/template/`，作为 coding 的起点；由智能体根据本次相册完成数据接入、文案、布局调整、测试和预览，而不是让用户手工改模板。

## 默认执行流程

1. **识别相册并连接夸克。** 读取当前环境中已安装的 `quarkclouddrive` Skill，按其说明检查授权并定位用户指定的相册/照片文件夹。详细衔接见 [夸克网盘到网站](references/quark-workflow.md)。沿用已明确的账号、文件夹与项目，不重复追问。仅当名称缺失、候选重名或授权缺失时补问必要信息。
2. **读取完整照片集合。** 获取指定范围的完整列表，把图片下载到独立的私人素材目录。更新时比较新旧批次，不把预览中的几条当作全部照片。这里的“整理”是制作网站，不是操作网盘目录；不调用云端相册重组来代替网站制作。
3. **准备工程并 coding。** 新建项目时运行 `python scripts/scaffold.py <新项目目录>`，在项目中安装依赖；不要把用户素材写进 Skill 安装目录。更新时读取已有 `src/artworks.js`、`src/categories.js` 与选片记录，保留用户确认过的文案和交互。智能体负责执行命令、创建 Python 环境、准备图像依赖和修复工程问题，不把这些步骤转交给用户。
4. **看图、选片、分类、写日记。** 按下文规则处理真实照片，由智能体生成私人选片清单并调用导入脚本。普通取舍自行完成，不要求用户逐张批准或手写 JSON。网盘原片保持不变。
5. **把网站运行起来。** 根据相册内容修改网页标题、分类封面、布局和文案，安装手势模型，完成测试与构建，启动本地服务并在浏览器验证。若环境不支持本地服务，说明具体限制并提供完整可运行项目；不要称空模板为已完成的网站。
6. **交付旅行记忆。** 给用户可打开的本地预览、选片数量与分类概览。以后用户说“相册又更新了”，重新读取该相册并跨批次去重，继续更新同一个网站。公开部署不是默认步骤。

用户明确提供本地照片时也可以直接处理，跳过夸克连接；这只是替代入口，不应在夸克已装好时要求用户手动下载或重新上传照片。

## 选片

运行 `python scripts/review_photos.py <照片目录> <独立的审阅输出目录>` 生成索引与联系表。Python 依赖见 `requirements.txt`。HEIC 在 macOS 可用系统转换器，其他平台需 `pillow-heif`。

查看全部联系表，再放大人物面部、近似连拍和裁切候选。联系表不能证明清晰度。默认优先竖图；先应用 EXIF 方向，再判断横竖。横图默认排除，只有用户同意且主体能完整保留时，才在清单中提供人工检查过的竖幅裁切。

同场景连拍综合清晰度、神情、姿态、遮挡和构图选一张。不要仅因背景相同就删掉不同场景；合理的剪影、背景虚化也不是失败照片。比较跨批次重复、带注释版本、拼图里的重复画面。保留本地的入选/排除理由，原片不变。

每张照片只归属一个分类。按实际素材使用清楚的类名，例如海景、建筑、街景、夜景、涂鸦、花草、单人照、合照；这些是例子，不是必须凑齐的列表。照片少时少分类，素材丰富时通常 6–10 类能形成饱满圆环。避免为了凑数复制照片或给极小的主题取夸张名字。每类选择一张清晰的代表图作为封面；封面可以同时是该类长卷中的照片。

## 导入与文案

阅读 [照片清单格式](references/manifest.md)，由视觉审阅结果编写清单，然后运行：

```bash
python scripts/import_photos.py selection.json --project <相册项目目录>
```

脚本校正方向、检查精确重复、拒绝未裁切的横图、生成去除 EXIF 的 WebP、更新相册数据。它不会判断近似照片、好看的表情或语义类别；这些仍需人工/视觉判断。

标题可简短文艺，分类名保持直观。日记围绕真实画面，人物心情可以用「像是」「也许」表达推测，不编造地点或当事人的经历。默认原创；不要把自行编写的句子署名给名家。已有文案除非照片替换或用户要求，否则保留。

## 页面与交互

沿用模板的米白背景、轻阴影、细字分类、大留白与低幅度动效。长卷按真实竖幅比例等高排布；照片详情展示完整图片，合照不要裁掉两侧的人。按需修改网页标题和各分类简介。

- 圆环：拖动/左右键旋转，滚轮/双指缩放；选中分类进入长卷。
- 长卷：拖动、滚轮、左右键浏览；返回保留圆环状态，再进入同类保留进度。
- 手势：圆环中手掌左右移动旋转、双掌距离缩放；长卷中手掌左右移动滑动。食指停留选中分类、照片、关闭或返回按钮。
- 开启照片详情时阻止手势继续推动底层内容。手势选择不应被上一次鼠标拖动的防误触状态拦住。

默认在项目中执行 `npm run setup:hands` 准备手势功能；用户明确不要手势时可跳过。该命令从 Google 官方存储下载固定版本模型并校验哈希。鼠标/触屏浏览不依赖模型。用户点击开关后才申请摄像头，关闭后释放轨道；需要 HTTPS 或 localhost。

## 验收与交付

执行 `npm test`、`npm run build`。在浏览器检查桌面与手机：圆环成形、分类入口可见、长卷可拖动、详情完整、返回与进度记忆正常。按改动范围使用 `evidence/interaction-check.html` 检查控制器；它是合成关键点测试，不能称为真人摄像头实测。更多结构说明见 [模板维护](references/template.md)。

报告入选数量、分类、重复处理与实际验证范围。默认交付本地预览。只有用户明确要求发布时才部署；开源模板和发布个人相册是不同操作，不把照片、选片清单、凭据或审阅截图顺带上传。
